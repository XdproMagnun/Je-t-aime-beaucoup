# Para Yess

Pagina romantica hecha en HTML, CSS y JavaScript para Yess.

## Como abrirla

Abre `index.html` en tu navegador.

## Como subirla a GitHub Pages

1. Crea un repositorio nuevo en GitHub.
2. Sube todos los archivos de esta carpeta.
3. En el repositorio, ve a `Settings` > `Pages`.
4. En `Build and deployment`, elige `Deploy from a branch`.
5. Selecciona la rama `main` y la carpeta `/root`.
6. Guarda los cambios.

La pagina principal es `index.html`.

## Nota sobre la musica

El archivo `something-about-us.mp3` esta incluido localmente. Algunos navegadores bloquean la reproduccion automatica con sonido; si eso pasa, usa el boton `Reproducir musica`.
